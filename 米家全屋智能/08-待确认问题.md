# 08 - 待确认问题

当前文件用于记录尚未最终确认、不宜直接写入正式方案的事项。

## 传感器点位与调试

已确认项见 `09-传感器安装与调试.md`（S-001 ~ S-015）。

### 所有传感器已确认

所有传感器（S-001 ~ S-015）的安装参数与调试策略已确认，详见 `09-传感器安装与调试.md`。

### 已确认通用调试项

- 主卧起夜链路中 `S-009` 与 `S-015` 的组合触发与抗误触策略（已确认）
- ES5 的模式选择：雷达模式 / 融合模式在各空间的适用原则（待实测调整）
- ES5 的区段屏蔽、干扰扫描、灵敏度、无人延时等调试基线（已确认基础参数）
- 客厅阳台、吧台、主卧床边等高风险区域的防猫误触调试方案（已确认）

## 次卫后续升级

- 是否补充次卫专用传感器
- 若补充传感器，次卫是否升级为自动化空间
- 次卫夜间低亮是否需要独立自动化规则

## 灯光与场景细节（已确认）

- `洗澡`、`起夜`、`睡觉` 对 `mood_lighting_mode` 的优先级定义已统一：洗澡/起夜对应安静模式，睡觉对应安静模式（见06-落地框架.md优先级表格）
- `晚餐` 对客厅的影响范围和亮度目标已细化：客厅灯光切换到温馨模式（2700K/40%），窗帘灯带除外（见03-场景定义.md）
- `起夜` 的无人退出时长保持1分钟，待实测确认是否需要调整

## 场景自动化逻辑（进行中）

### 已确认内容

- 平台职责划分：米家APP负责场景预设、设备管理；极客版负责场景自动化、状态变量管理、条件判断
- 自动化逻辑框架：触发条件 → 触发条件检查 → 状态变量更新 → 条件动作（条件检查+执行动作） → 退出条件
- 灯光氛围模式已重新梳理：正常模式（4000K/70%）、明亮模式（5000K/100%）、温馨模式（3000K/50%）、安静模式（2700K/20%）、夜间模式（2700K/5%）
- 灯光氛围操作矩阵已优化：场景退出时重新计算mood，避免多场景冲突
- 场景-mood映射表已创建，明确每个场景对应的全局灯光mood和优先级

### 待确认内容

- 极客版功能调查：需要确认极客版是否支持变量存储、子场景封装、参数传递等能力（网络搜索暂不可用）
- 回家场景自动化逻辑已初步编写，待极客版功能确认后调整
- 其余10个场景自动化逻辑待补充

## 落地验证

- 所有本地环境传感器的型号、数量、安装位置与接入方式
- 开窗器联动所需的雨水传感器、室外温湿度传感器布点
- 各空间场景与灯组映射表的最终校对
